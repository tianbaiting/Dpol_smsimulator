# 1. 初始化 micromamba 的 shell 挂钩 (保持你原有的)
if command -v micromamba >/dev/null 2>&1; then
    eval "$(micromamba shell hook --shell zsh)"
fi

# 2. 【新增】这里是关键！显式激活你的环境
micromamba activate anaroot-env

# 3. 加载项目设置 (保持你原有的，通常用于设置 Geant4 数据路径等)
if [[ -f ./setup.sh ]]; then
    source ./setup.sh
fi